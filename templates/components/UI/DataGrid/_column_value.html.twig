{% set template = column.template %}

{% include ['components/UI/DataGrid/column_type/' ~ template, grid.theme ~ '/column_type/' ~ template, template] with {
    'column': column,
    'item': item,
    'params': column.params,
} %}
