<header>
    <div class="logo">
        <img src="{{ asset('images/logo_white_bg.png') }}" width="110" height="32" alt="Wiltec"
            class="navbar-brand-image">
    </div>
    {# <div>
        <p>Current locale: {{ app.request.locale }}</p>
        <p>Session locale: {{ app.session ? app.session.get('_locale') : 'no session' }}</p>
        <p>Supported locales: {{ get_supported_locales()|join(', ') }}</p>
    </div> #}

    {# {{ component('LanguageSelector') }} #}


</header>
<nav style="height:3rem; background: #80aab6"></nav>